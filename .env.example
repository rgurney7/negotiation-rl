# Copy to .env and fill in. .env is git-ignored.
OPEN_AI_API_KEY=        # gpt-5.4-nano (grade/eval buyer)
GOOGLE_API_KEY=         # gemini-2.5-flash (train buyer)
HF_TOKEN=               # durable checkpoint mirror to a private HF repo
RUNPOD_API_KEY=         # provision/stop the GPU pod (runpodctl)
